#include "holberton.h"

/**
 * _atoi - converts a character to an integer
 * @s: input character string
 * Return: returns 0 for none or integer value of int in string
 */
int _atoi(char *s)
{
	if (s == NULL)
		return (0);
	return (0);
}
